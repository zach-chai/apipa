json = { data: [] }
messages.each do |message|
  json[:data] << {
    type: 'messages',
    id: message.id.to_i,
    attributes: {
      content: message.content,
      is_palindrome: message.is_palindrome == 'true'
    }
  }
end
